#### docker-compose cmd

sudo docker compose up -d --build
Используешь когда:

первый запуск

поменяла Dockerfile

поменяла скрипты (.sh)

что-то сломалось и хочешь пересобрать

docker compose ps        # посмотреть, что запущено
docker compose logs      # посмотреть логи
docker compose logs -f   # логи в реальном времени
docker compose down      # остановить и удалить контейнеры
docker compose down -v   # + удалить volumes (ОСТОРОЖНО)

docker compose logs nginx
docker compose logs mariadb
docker compose logs wordpress

docker compose restart nginx # рестартануть контейнер


docker compose down

Что делает:

    останавливает контейнеры

    удаляет контейнеры

    НЕ удаляет volumes

📌 Это безопасная команда

docker compose down -v

Удаляет:

    контейнеры

    сети

    volumes (БД, данные WordPress)

Используй только если:

    БД сломалась

    нужно проверить инициализацию с нуля

    на защите по просьбе проверяющего


Зайти внутрь контейнера (очень важно)
docker compose exec nginx sh
docker compose exec mariadb sh
docker compose exec wordpress sh

Проверить сети и volumes
docker network ls
docker volume ls

Команды для проверки «как у проверяющего»
Остановить VM → снова поднять
docker compose up -d


Если всё работает без --build → 👍 конфигурация правильная.

Проверить автоперезапуск
docker inspect nginx | grep Restart

❌ Чего НЕ надо для Inception

🚫 docker run
🚫 docker build
🚫 docker rm -f
🚫 docker stop (лучше compose down)

На защите это минус, если используешь вместо compose.

🧾 Мини-чеклист Inception

Ты готова, если умеешь:

объяснить docker compose up -d --build

показать docker compose ps

открыть логи конкретного сервиса

зайти в контейнер через exec

снести всё через down -v и поднять заново

объяснить, что лежит в volumes
